name: Scrape Jobs

on:
  schedule:
    # Run daily at 6:00 AM UTC
    - cron: '0 6 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run scraper
        run: npm run scrape
        env:
          NODE_ENV: production

      - name: Run hygiene (expire dead listings)
        # Runs right after the scrape so the "disappeared from source" signal is
        # fresh. Probes only unseen jobs; a job needs 2 consecutive dead (404/410)
        # checks across runs before it's expired, so this safely runs daily.
        run: npm run hygiene
        env:
          NODE_ENV: production

      - name: Check for changes
        id: git-check
        run: |
          git diff --exit-code src/data/jobs.json src/data/expired-slugs.json || echo "changes=true" >> $GITHUB_OUTPUT

      - name: Commit and push changes
        if: steps.git-check.outputs.changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add src/data/jobs.json src/data/expired-slugs.json
          git commit -m "Update job listings + hygiene - $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
